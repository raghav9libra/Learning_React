// useReducer(reducer, initialState) hook accept 2 arguments:
//  the reducer function and the initial state.
//  The hook then returns an array of 2 items:
// the current state and the dispatch function.

// const [state, dispatch] = useReducer(reducer, initialState);
